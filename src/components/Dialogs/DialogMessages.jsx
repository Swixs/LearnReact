const Messages = (props) => {
    return (
        <div>
            <div>
                <div className="User">{props.message}</div>
            </div>
        </div >
    )
}

export default Messages
